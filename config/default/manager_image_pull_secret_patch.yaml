# This patch adds the possibility to use a pull secret for the manager image,
# which is required for pulling from GHCR.
# The pull secret is created in the Makefile and added to the manager deployment
# using this patch.
- op: add
  path: /spec/template/spec/imagePullSecrets
  value:
  - name: saap-dockerconfigjson